# Test
spring:
    profiles:
        active: local # ?? ?? ??
    jpa:
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL5InnoDBDialect
# local ??
---
spring:
    profiles: local
    jpa:
        show-sql: true